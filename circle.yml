test:
  override:
    - cmake --version
    - pwd
    - ls -l
    - mkdir build && cd build && cmake -DBUILD_SHARED=ON .. && make
    - tar -zcvf VulkanSamples-linux64.tar.gz build/demos
general:
  artifacts:
    - "VulkanSamples.tar.gz"
